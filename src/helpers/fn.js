import { questionHistoria } from '../data'
export const dificultyQuestion =(category)=>{
    if()
}
export const categoryQuestions = (category) => {
  if (category === 'historia') {
    console.log('historia')
    return questionHistoria
  }
}
